<?xml version="1.0" encoding="utf-8" ?>
<ViewCell x:Class="SillyCompany.Mobile.Practices.Presentation.Views.SillyCell"
          xmlns="http://xamarin.com/schemas/2014/forms"
          xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
          xmlns:abstractions="clr-namespace:ImageCircle.Forms.Plugin.Abstractions;assembly=ImageCircle.Forms.Plugin"
          xmlns:vmo="clr-namespace:SillyCompany.Mobile.Practices.Presentation.ViewModels;assembly=SillyCompany.Mobile.Practices">

    <StackLayout Orientation="Horizontal"
                 Padding="16,16,16,16"
                 x:DataType="vmo:SillyDudeVmo">

        <abstractions:CircleImage Style="{StaticResource SmallAvatar}"
                                  Aspect="AspectFill"
                                  Source="{Binding ImageUrl}" />

        <StackLayout Orientation="Vertical" Padding="16,0,0,0">
            <Label Style="{StaticResource TextSubhead}" Text="{Binding Name}" />
            <Label Style="{StaticResource TextSubheadSecondary}" Text="{Binding Role}" />
        </StackLayout>

    </StackLayout>
</ViewCell>